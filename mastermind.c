#include <stdio.h>

#define patternSize 8

int generatePattern()
{
    int pattern[patternSize];
    for (int i = 0; i < patternSize; i++)
    {
        pattern[i] = i + 1;
    }
    for (int i = patternSize - 1; i > 0; i--)
    {
        int j =
    }
    return pattern;
}

int main()
{
    return 0;
}